const config = require('../../config.json');
const fs = require("fs");
const pathGlobaleData = config.location + "/storage/data/GlobaleData.json";
const pathSpecialsChannel = config.location + "/storage/data/specialChannelList/";




module.exports.run = async (bot, reaction, user, action) => {
    
}

module.exports.help = {
    name: "index"
};